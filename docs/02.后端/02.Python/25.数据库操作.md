---
title: 数据库操作
date: 2026-09-03 15:11:09
permalink: /pages/python-adv-05/
categories:
  - 后端
  - Python
tags:
  - Python
author:
  name: lious
  link: https://github.com/l552177239
---

# 数据库操作

## 1. SQLite 基础

```python
import sqlite3

# 连接数据库（不存在则创建）
conn = sqlite3.connect("mydb.sqlite")
cursor = conn.cursor()

# 创建表
cursor.execute("""
    CREATE TABLE IF NOT EXISTS users (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        age INTEGER,
        email TEXT UNIQUE
    )
""")

# 插入数据
cursor.execute("INSERT INTO users (name, age, email) VALUES (?, ?, ?)",
               ("Alice", 25, "alice@example.com"))

# 批量插入
users = [
    ("Bob", 30, "bob@example.com"),
    ("Charlie", 35, "charlie@example.com")
]
cursor.executemany("INSERT INTO users (name, age, email) VALUES (?, ?, ?)", users)

# 提交事务
conn.commit()

# 查询数据
cursor.execute("SELECT * FROM users WHERE age > ?", (25,))
rows = cursor.fetchall()
for row in rows:
    print(row)

# 使用字典形式返回
cursor.row_factory = sqlite3.Row
cursor.execute("SELECT * FROM users")
for row in cursor.fetchall():
    print(dict(row))

# 关闭连接
conn.close()

# 使用上下文管理器
with sqlite3.connect("mydb.sqlite") as conn:
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM users")
    print(cursor.fetchall())
```

## 2. 使用 ORM：SQLAlchemy

```python
from sqlalchemy import create_engine, Column, Integer, String
from sqlalchemy.orm import declarative_base, sessionmaker

Base = declarative_base()

# 定义模型
class User(Base):
    __tablename__ = "users"
    
    id = Column(Integer, primary_key=True)
    name = Column(String(50), nullable=False)
    age = Column(Integer)
    email = Column(String(100), unique=True)
    
    def __repr__(self):
        return f"<User(name='{self.name}', age={self.age})>"

# 创建引擎和表
engine = create_engine("sqlite:///mydb.sqlite", echo=True)
Base.metadata.create_all(engine)

# 创建会话
Session = sessionmaker(bind=engine)
session = Session()

# 增
new_user = User(name="Alice", age=25, email="alice@example.com")
session.add(new_user)
session.commit()

# 查
user = session.query(User).filter_by(name="Alice").first()
print(user)

# 改
user.age = 26
session.commit()

# 删
session.delete(user)
session.commit()

# 复杂查询
results = session.query(User).filter(User.age > 20).order_by(User.age.desc()).all()

# 关闭会话
session.close()
```
