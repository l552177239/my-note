---
title: 经典示例数据
date: 2026-09-05 10:40:00
permalink: /pages/mysql-examples/
categories:
  - 后端
  - MySQL
tags:
  - MySQL
  - SQL
author:
  name: lious
  link: https://github.com/l552177239
---

# 经典示例数据

提供部门 / 员工建表脚本与常见练习题，便于动手验证前面章节的语法。

<!-- more -->

## 1. 建表脚本

```sql
-- 部门表
CREATE TABLE departments (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    dept_name   VARCHAR(50) NOT NULL,
    location    VARCHAR(50),
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 员工表
CREATE TABLE employees (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(50) NOT NULL,
    gender      CHAR(1) DEFAULT 'M',
    age         INT,
    salary      DECIMAL(10,2),
    dept_id     INT,
    manager_id  INT,
    hire_date   DATE,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (dept_id) REFERENCES departments(id)
);

-- 插入部门数据
INSERT INTO departments (dept_name, location) VALUES
('技术部', '北京'),
('市场部', '上海'),
('财务部', '深圳');

-- 插入员工数据
INSERT INTO employees (name, gender, age, salary, dept_id, manager_id, hire_date) VALUES
('张三', 'M', 28, 15000.00, 1, NULL, '2022-03-15'),
('李四', 'F', 25, 12000.00, 1, 1, '2023-06-01'),
('王五', 'M', 30, 18000.00, 1, 1, '2021-01-10'),
('赵六', 'F', 26, 10000.00, 2, NULL, '2023-09-20'),
('孙七', 'M', 24, 8000.00, 2, 4, '2024-01-05'),
('周八', 'F', 35, 20000.00, 3, NULL, '2020-05-12');
```

## 2. 经典练习题

```sql
-- 1. 查询每个部门的平均薪资
SELECT d.dept_name, AVG(e.salary) AS avg_salary
FROM employees e
JOIN departments d ON e.dept_id = d.id
GROUP BY d.dept_name;

-- 2. 查询薪资高于部门平均薪资的员工
SELECT e.name, e.salary, d.dept_name
FROM employees e
JOIN departments d ON e.dept_id = d.id
WHERE e.salary > (
    SELECT AVG(salary) FROM employees WHERE dept_id = e.dept_id
);

-- 3. 查询每个部门薪资最高的员工
SELECT e.name, e.salary, d.dept_name
FROM employees e
JOIN departments d ON e.dept_id = d.id
WHERE (e.dept_id, e.salary) IN (
    SELECT dept_id, MAX(salary) FROM employees GROUP BY dept_id
);

-- 4. 查询入职超过2年的员工
SELECT name, hire_date, DATEDIFF(NOW(), hire_date) / 365 AS 工作年限
FROM employees
WHERE hire_date <= DATE_SUB(NOW(), INTERVAL 2 YEAR);

-- 5. 查询没有员工的部门
SELECT d.dept_name
FROM departments d
LEFT JOIN employees e ON d.id = e.dept_id
WHERE e.id IS NULL;
```
