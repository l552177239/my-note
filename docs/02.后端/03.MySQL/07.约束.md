---
title: 约束
date: 2026-09-05 10:30:00
permalink: /pages/mysql-constraints/
categories:
  - 后端
  - MySQL
tags:
  - MySQL
  - SQL
author:
  name: lious
  link: https://github.com/l552177239
---

# 约束

约束用于保证数据完整性与合法性。

<!-- more -->

| 约束 | 关键字 | 说明 |
|------|--------|------|
| 主键 | `PRIMARY KEY` | 唯一标识每行，非空且唯一 |
| 外键 | `FOREIGN KEY` | 关联其他表的主键 |
| 唯一 | `UNIQUE` | 列值不能重复 |
| 非空 | `NOT NULL` | 列值不能为空 |
| 默认值 | `DEFAULT` | 未指定时自动填充 |
| 检查 | `CHECK` | 限制列值范围 |

```sql
CREATE TABLE orders (
    order_id    INT PRIMARY KEY AUTO_INCREMENT,
    user_id     INT NOT NULL,
    order_no    VARCHAR(50) UNIQUE NOT NULL,
    status      TINYINT DEFAULT 0,
    amount      DECIMAL(10,2) CHECK (amount >= 0),
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    -- 外键约束
    CONSTRAINT fk_user 
        FOREIGN KEY (user_id) 
        REFERENCES users(id)
        ON DELETE CASCADE    -- 级联删除
        ON UPDATE CASCADE    -- 级联更新
);
```
