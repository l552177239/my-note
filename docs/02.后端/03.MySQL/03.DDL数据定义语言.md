---
title: DDL数据定义语言
date: 2026-09-05 10:10:00
permalink: /pages/mysql-ddl/
categories:
  - 后端
  - MySQL
tags:
  - MySQL
  - SQL
author:
  name: lious
  link: https://github.com/l552177239
---

# DDL 数据定义语言

DDL 用于定义数据库结构，常用语句包括 `CREATE`、`ALTER`、`DROP`、`TRUNCATE`。

<!-- more -->

## 1. 数据库操作

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS mydb 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- 查看所有数据库
SHOW DATABASES;

-- 使用数据库
USE mydb;

-- 删除数据库
DROP DATABASE IF EXISTS mydb;
```

## 2. 表操作

```sql
-- 创建表
CREATE TABLE IF NOT EXISTS employees (
    id          INT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    name        VARCHAR(50) NOT NULL COMMENT '姓名',
    gender      CHAR(1) DEFAULT 'M' COMMENT '性别',
    age         INT CHECK (age >= 0) COMMENT '年龄',
    salary      DECIMAL(10,2) COMMENT '薪资',
    dept_id     INT COMMENT '部门ID',
    hire_date   DATE COMMENT '入职日期',
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) COMMENT='员工表';

-- 查看表结构
DESC employees;
SHOW CREATE TABLE employees;

-- 修改表
ALTER TABLE employees ADD COLUMN email VARCHAR(100);
ALTER TABLE employees MODIFY COLUMN age TINYINT;
ALTER TABLE employees DROP COLUMN email;
ALTER TABLE employees RENAME TO staff;

-- 删除表
DROP TABLE IF EXISTS employees;

-- 清空表（重置自增ID）
TRUNCATE TABLE employees;
```
